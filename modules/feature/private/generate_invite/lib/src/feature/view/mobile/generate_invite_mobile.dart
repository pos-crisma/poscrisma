import 'package:flutter/material.dart';

class GenerateInviteMobile extends StatelessWidget {
  const GenerateInviteMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
