import 'package:flutter/material.dart';

class TeamWeb extends StatelessWidget {
  const TeamWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
