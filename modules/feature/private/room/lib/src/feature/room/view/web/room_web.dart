import 'package:flutter/material.dart';

class RoomWeb extends StatelessWidget {
  const RoomWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
