import 'package:flutter/material.dart';

class RoomSearchWeb extends StatelessWidget {
  const RoomSearchWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
