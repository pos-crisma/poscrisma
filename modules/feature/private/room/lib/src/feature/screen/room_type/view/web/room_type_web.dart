import 'package:flutter/material.dart';

class RoomTypeWeb extends StatelessWidget {
  const RoomTypeWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
