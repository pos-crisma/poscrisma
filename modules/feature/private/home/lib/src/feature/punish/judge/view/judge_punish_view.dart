import 'package:flutter/material.dart';

class JudgePunish extends StatelessWidget {
  const JudgePunish({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
