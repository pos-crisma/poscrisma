import 'package:flutter/material.dart';

class YoungMobile extends StatelessWidget {
  YoungMobile({super.key});

  final ScrollController controller = ScrollController();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
